(() => {

    const numbers : number[] = [1,2,3,4,5,5,5,5,]

    const villains = ['Omega Rojo', 'Dormamu', 'Duende Verde']

    villains.forEach(v => console.log(v.toUpperCase()))
    numbers.forEach(v => console.log(v))

})()